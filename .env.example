# RK Inversiones — generado por npm run seed (no editar a mano)
#
# Configuración:
#   npm run seed              → genera .env + aplica migraciones DB
#
# Secretos en el VPS (no van a git):
#   .smtp.local               → clave SMTP (info@renace.tech)
#   .evolution.local          → Evolution API (WhatsApp automático)
#
# Valores por defecto: scripts/seed.defaults.sh
